require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

RSpec.describe GearUsesController, type: :controller do

  # This should return the minimal set of attributes required to create a valid
  # GearUse. As you add validations to GearUse, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # GearUsesController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET #index" do
    it "assigns all gear_uses as @gear_uses" do
      gear_use = GearUse.create! valid_attributes
      get :index, params: {}, session: valid_session
      expect(assigns(:gear_uses)).to eq([gear_use])
    end
  end

  describe "GET #show" do
    it "assigns the requested gear_use as @gear_use" do
      gear_use = GearUse.create! valid_attributes
      get :show, params: {id: gear_use.to_param}, session: valid_session
      expect(assigns(:gear_use)).to eq(gear_use)
    end
  end

  describe "GET #new" do
    it "assigns a new gear_use as @gear_use" do
      get :new, params: {}, session: valid_session
      expect(assigns(:gear_use)).to be_a_new(GearUse)
    end
  end

  describe "GET #edit" do
    it "assigns the requested gear_use as @gear_use" do
      gear_use = GearUse.create! valid_attributes
      get :edit, params: {id: gear_use.to_param}, session: valid_session
      expect(assigns(:gear_use)).to eq(gear_use)
    end
  end

  describe "POST #create" do
    context "with valid params" do
      it "creates a new GearUse" do
        expect {
          post :create, params: {gear_use: valid_attributes}, session: valid_session
        }.to change(GearUse, :count).by(1)
      end

      it "assigns a newly created gear_use as @gear_use" do
        post :create, params: {gear_use: valid_attributes}, session: valid_session
        expect(assigns(:gear_use)).to be_a(GearUse)
        expect(assigns(:gear_use)).to be_persisted
      end

      it "redirects to the created gear_use" do
        post :create, params: {gear_use: valid_attributes}, session: valid_session
        expect(response).to redirect_to(GearUse.last)
      end
    end

    context "with invalid params" do
      it "assigns a newly created but unsaved gear_use as @gear_use" do
        post :create, params: {gear_use: invalid_attributes}, session: valid_session
        expect(assigns(:gear_use)).to be_a_new(GearUse)
      end

      it "re-renders the 'new' template" do
        post :create, params: {gear_use: invalid_attributes}, session: valid_session
        expect(response).to render_template("new")
      end
    end
  end

  describe "PUT #update" do
    context "with valid params" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested gear_use" do
        gear_use = GearUse.create! valid_attributes
        put :update, params: {id: gear_use.to_param, gear_use: new_attributes}, session: valid_session
        gear_use.reload
        skip("Add assertions for updated state")
      end

      it "assigns the requested gear_use as @gear_use" do
        gear_use = GearUse.create! valid_attributes
        put :update, params: {id: gear_use.to_param, gear_use: valid_attributes}, session: valid_session
        expect(assigns(:gear_use)).to eq(gear_use)
      end

      it "redirects to the gear_use" do
        gear_use = GearUse.create! valid_attributes
        put :update, params: {id: gear_use.to_param, gear_use: valid_attributes}, session: valid_session
        expect(response).to redirect_to(gear_use)
      end
    end

    context "with invalid params" do
      it "assigns the gear_use as @gear_use" do
        gear_use = GearUse.create! valid_attributes
        put :update, params: {id: gear_use.to_param, gear_use: invalid_attributes}, session: valid_session
        expect(assigns(:gear_use)).to eq(gear_use)
      end

      it "re-renders the 'edit' template" do
        gear_use = GearUse.create! valid_attributes
        put :update, params: {id: gear_use.to_param, gear_use: invalid_attributes}, session: valid_session
        expect(response).to render_template("edit")
      end
    end
  end

  describe "DELETE #destroy" do
    it "destroys the requested gear_use" do
      gear_use = GearUse.create! valid_attributes
      expect {
        delete :destroy, params: {id: gear_use.to_param}, session: valid_session
      }.to change(GearUse, :count).by(-1)
    end

    it "redirects to the gear_uses list" do
      gear_use = GearUse.create! valid_attributes
      delete :destroy, params: {id: gear_use.to_param}, session: valid_session
      expect(response).to redirect_to(gear_uses_url)
    end
  end

end
