<%= simple_form_for(@gear_use) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :gear_set %>
    <%= f.association :gear %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
